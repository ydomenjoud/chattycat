---
tags: serie
pagination:
    data: series
    size: 1
    alias: serie
    addAllPagesToCollections: true
eleventyComputed:
    name: "{{serie.name}}"
    image: "{{serie.image}}"
    resume: "{{serie.resume}}"
    description: "{{serie.description}}"
permalink: "series/{{serie.slug}}.html"
---

<div id="serie_presentation">
    <div>
        {%- if image %}
            {% remoteImage  image, "plop", {widths: [312]} %}
        {%- endif %}
    </div>
    
    <div>
        <h1>{{name}}</h1>
    </div>    
</div>
{# 
<div id="book_presentation2">

    
    <p></p>

    <div>
        <div id="themes">
            {% set list = themes.split(', ') %}
            {%- for theme in list %}
            <p>{{theme}}</p>
            {%- endfor %}
        </div>
        <h4>Résumé</h4>
        <p>{{summary}}</p>
    </div>
</div>

<div id="book_badges">
    <div>
        <h3>{{age}}<span> ans</span></h3>
        <span>v</span>
    </div>
    <div>
        <h3>EXTRAIT</h3>
    </div>
    <div>
        <button>LIVRE AUDIO</button>
    </div>
</div> #}
