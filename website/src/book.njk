---
tags: books
pagination:
    data: global.books
    size: 1
    alias: book
    addAllPagesToCollections: true
eleventyComputed:
    title: "{{book.name}}"
permalink: "books/{{ book.slug }}.html"
---


{% set collection = global.series | find(book.collection) %}

<div class="container">
    <div id="book_presentation">
        <div>
            {%- if book.image %}
                {% remoteImage book.image, book.name, {widths: [312]} %}
                {% remoteImage book.image, book.name, {widths: [90]} %}
            {%- endif %}
        </div>

        <div>
            <div>
                <h1>{{ collection.name if collection else ''  }}</h1>
                <h2>{{ book.name }}</h2>
                <h5>
                    {%- for authorId in book.authors %}
                        {% set author = global.authors | find(authorId) %}
                        <a href="/authors/{{author.slug}}.html">{{ author.name }}</a>
                    {%- endfor %}
                </h5>
                <h5>
                    {%- for authorId in book.illustrators %}
                        {% set author = global.authors | find(authorId) %}
                        <a href="/authors/{{author.slug}}.html">{{ author.name }}</a>
                    {%- endfor %}
                </h5>
            </div>
            <div>
                <h3>{{ book.introduction }}</h3>
            </div>
        </div>
    </div>

    <div id="book_presentation_bottom">
        <div id="book_presentation2">
            <div>
                <div id="themes">
                    {% if book.keywords %}
                        {% set list = book.keywords.split(', ') %}
                        {%- for theme in list %}
                            <p>{{theme}}</p>
                        {%- endfor %}
                    {%- endif %}
                </div>
                <h4>Résumé</h4>
                <p>{{ book.summary }}</p>
            </div>
        </div>
        <div id="book_badges">
            <a class="age_cat" href="/ages/{{ book.age_cat }}.html">
                <span>
                    {{ book.age }}
                </span>
                <span class="ico">v</span>
            </a>
            {% if book.calameo %}
            <div class="calameo">
                <h3><a href="{{ book.calameo }}" target="_blank">EXTRAIT</a></h3>
            </div>
            {% endif %}
            {% if book.soundcloud %}
            <div class="soundcloud">
                <a href="{{ book.soundcloud }}" target="_blank" class="button">LIVRE AUDIO</a>
            </div>
            {% endif %}
        </div>
    </div>

    <section id="book_informations">

        <article id="links" >
            <h5>Où trouver ce livre ?</h5>
            <ul>
                <li ><a href="">Parislibrairies.fr</a></li>
                <li ><a href="">Placedeslibraires.fr</a></li>
                <li ><a href="">Sauramps</a></li>
                <li ><a href="">leslibraires.fr</a></li>
                <li ><a href="">Amazon</a></li>
                <li ><a href="">la Fnac</a></li>
                <li ><a href="">Cultura</a></li>
                <li ><a href="">Decitre</a></li>
                <li ><a href="">Furet du Nord</a></li>
                <li ><a href="">Eleclerc</a></li>
                <li ><a href="">Chapitre</a></li>
                <li ><a href="">La Procure</a></li>
                <li ><a href="">LIbrairie Dialogue</a></li>
                <li ><a href="">Libraires Ensembles</a></li>
            </ul>
        </article>

        <article id="caracs">
            <h4>Caractéristiques</h4>
           <table>
               {% if book.name %}
               <tr>
                   <td>Titre</td>
                   <td>{{ book.name }}</td>
               </tr>
               {% endif %}
               {% if book.authors %}
               <tr>
                   <td>Auteur(s)</td>
                   <td>
                       {%- for authorId in book.authors %}
                            {% set author = global.authors | find(authorId) %}
                            <a href="/authors/{{author.slug}}.html">{{ author.name }}</a>
                       {%- endfor %}
                   </td>
               </tr>
               {% endif %}
               {% if book.illustrators %}
               <tr>
                   <td>Illustrateur(s)</td>
                   <td>
                       {%- for authorId in book.illustrators %}
                           {% set author = global.authors | find(authorId) %}
                           <a href="/authors/{{author.slug}}.html">{{ author.name }}</a>
                       {%- endfor %}
                   </td>
               </tr>
               {% endif %}
               {% if book.collection %}
               <tr>
                   <td>Série</td>
                   <td>
                       {% set collection = global.series | find(book.collection) %}
                       <a href="/series/{{collection.slug}}.html">{{ collection.name }}</a>
                    </td>
               </tr>
               {% endif %}
               {% if book.genre %}
               <tr>
                   <td>Genre</td>
                   <td>{{ book.genre }}</td>
               </tr>
               {% endif %}
               {% if book.parution %}
               <tr>
                   <td>Date de parution</td>
                   <td>{{ book.parution | fdate  }}</td>
               </tr>
               {% endif %}
               {% if book.pages %}
               <tr>
                   <td>Nombre de pages</td>
                   <td>{{ book.pages}}</td>
               </tr>
               {% endif %}
               {% if book.sizes %}
               <tr>
                   <td>Dimensions</td>
                   <td>{{ book.sizes }}</td>
               </tr>
               {% endif %}
               {% if book.support %}
               <tr>
                   <td>Support</td>
                   <td>{{ book.support }}</td>
               </tr>
               {% endif %}
               {% if book.ean %}
               <tr>
                   <td>ISBN / EAN</td>
                   <td>{{ book.ean}}</td>
               </tr>
               {% endif %}
           </table>
        </article>
    </section>

</div>

{% if collection %}
    <section id="discover_collection">
        <a class="container"
           style="background-image: url('{{ collection.image }}')"
            href="/series/{{ collection.slug }}.html">
            Découvre la série !
            <span>{{ collection.name }}</span>
        </a>
    </section>
{% endif %}}
